---
- name: Deploy Maven App to Kubernetes
  hosts: localhost
  connection: local
  tasks:
    - name: Apply Kubernetes Manifests
      kubernetes.core.k8s:
        state: present
        src: "{{ item }}"
      loop:
        - ../k8s/deployment.yml
        - ../k8s/service.yml


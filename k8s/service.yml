apiVersion: v1
kind: Service
metadata:
  name: automatisation-service
spec:
  selector:
    app: automatisation-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: NodePort

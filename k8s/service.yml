apiVersion: v1
kind: Service
metadata:
  name: automatisation-service
  labels:
    app: automatisation-app
spec:
  selector:
    app: automatisation-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP

